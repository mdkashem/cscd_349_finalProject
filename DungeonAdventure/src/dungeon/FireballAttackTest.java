package dungeon;
/*
import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class FireballAttackTest {

	@Test
	void testAttack() {
		FireballAttack testFireballAttack = new FireballAttack("TestFireballAttack");
		Thief attacker = new Thief();
		Ogre defender = new Ogre();
		
		while(defender.getHitPoints() > 0)
		{
			testFireballAttack.attack(attacker, defender);
		}
		
		assertTrue(defender.getHitPoints() < 1);
	}

}
*/